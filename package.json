{
  "name": "minitracker-bot",
  "version": "1.0.0",
  "description": "Mini Stat tracker",
  "main": "main.ts",
  "author": "AKtheGREAT",
  "dependencies": {
    "@types/node": "^10.12.10",
    "discord.js": "^11.4.2",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "copyFiles": "copyfiles -u 1 src/**/*.json build/",
    "buildandrun": "npm run copyFiles && tsc && node build/main.js"
  },
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.1.0"
  },
  "keywords": []
}
